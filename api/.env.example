# Local dev defaults to SQLite; production points at PostgreSQL.
# DATABASE_URL=postgresql+psycopg://cerebra:cerebra@localhost:5432/cerebra
DATABASE_URL=sqlite+pysqlite:///./cerebra.db

# AI assessment (Phase 4). Without a key, review scoring falls back to a heuristic.
# ANTHROPIC_API_KEY=
ASSESSMENT_MODEL=claude-opus-4-8
